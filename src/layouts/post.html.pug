---
layout: default
---

article.post(class=document.status ? ('post_status_' + document.status) : '')
    h1.post__title
        != document.title

    .post__text
        != content

    .post__meta
        a.post__source(href=postSourceURL() target='_blank' rel='noopener')
            | Исходный код статьи

        .tags.post__tags
            each tag in document.tags
                span(class='tags__tag')= site.tagNames[tag] || tag

        time.post__date(datetime= pubDateForDatetimeAttribute(document.date))
            != pubDate(document.date)

        .post__share
            script(src="/vendor/likely/likely.js")
            link(rel="stylesheet" href="/vendor/likely/likely.css")

            .likely
              .twitter
              .facebook
              .vkontakte
              .telegram
              .odnoklassniki
              .linkedin
