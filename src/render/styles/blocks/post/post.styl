/*
 * Post
 */

.b-post {
    &__title {
        position: relative;
        margin: 2em 0 1em;
        font-weight: 300;

        a {
        }
    }

    &__text {
        img {
            max-width: 100%
            height: auto
            box-shadow: 0 0 0 5px #fff,
                        0 0 0 7px #eee;
        }
    }

    &__meta {
    }

    &__date {
        text-transform: uppercase;
        letter-spacing: 0.12em;
        font-size: 0.75em;
    }
}
