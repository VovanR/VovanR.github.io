.short-service {
    & + & {
        margin-top: 2em;
    }

    .js & {
        opacity: 1;
        transform: translate(0, 0);
        transition: opacity linear 0.25s,
                    transform ease 0.25s;
    }
}
